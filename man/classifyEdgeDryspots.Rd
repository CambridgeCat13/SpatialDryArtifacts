% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detectEdgeDryspots.R
\name{classifyEdgeDryspots}
\alias{classifyEdgeDryspots}
\title{Classify edge dryspots into categories for quality control (Enhanced Version)}
\usage{
classifyEdgeDryspots(
  spe,
  samples = "sample_id",
  min_spots = 20,
  low_umi_threshold = 100,
  removal_threshold = 0.5,
  name = "edge_dryspot",
  exclude_slides = NULL
)
}
\arguments{
\item{spe}{A SpatialExperiment object that has been processed with detectEdgeDryspots()}

\item{samples}{Character string specifying the sample ID column name (default: "sample_id")}

\item{min_spots}{Minimum number of spots required for a problem area to be considered (default: 20)}

\item{low_umi_threshold}{UMI count threshold below which spots are considered low quality (default: 100)}

\item{removal_threshold}{Proportion threshold for marking problem areas for removal (default: 0.5)}

\item{name}{Character string matching the name used in detectEdgeDryspots() (default: "edge_dryspot")}

\item{exclude_slides}{Character vector of slide IDs to exclude from edge detection (default: NULL)}
}
\value{
A SpatialExperiment object with additional classification columns in colData:
\item{\link{name}_true_edges}{Logical indicating edges after slide exclusion}
\item{\link{name}_binary}{Character classification: "edge", "problem area", or "none"}
\item{\link{name}_classification}{Character detailed classification: "edge", "remove", "flag", "small", or "none"}
\item{\link{name}_enhanced}{Character enhanced classification: "edge_dryspot", "interior_outlier", etc.}
\item{\link{name}_edge_zone}{Numeric/Character indicating edge zone: 1, 2, 3, or "interior"}
\item{\link{name}_sample_type}{Character sample type: "corner", "edge", or "interior"}
}
\description{
This function classifies detected edge dryspots and problem areas into
different categories based on size and quality metrics for downstream
filtering decisions. Enhanced with backward compatibility.
}
\examples{
# Basic usage (after running detectEdgeDryspots)
\dontrun{
spe <- classifyEdgeDryspots(spe)

# View enhanced classifications
table(spe$edge_dryspot_enhanced)
table(spe$edge_dryspot_edge_zone)
}

}
