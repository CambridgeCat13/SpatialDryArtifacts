% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detectEdgeDryspots.R
\name{detectEdgeDryspots}
\alias{detectEdgeDryspots}
\title{Detect edge dryspots in spatial transcriptomics data (Enhanced Version)}
\usage{
detectEdgeDryspots(
  spe,
  qc_metric = "sum_gene",
  samples = "sample_id",
  mad_threshold = 3,
  edge_threshold = 0.6,
  min_cluster_size = 20,
  shifted = FALSE,
  batch_var = "both",
  name = "edge_dryspot"
)
}
\arguments{
\item{spe}{A SpatialExperiment object containing spatial transcriptomics data}

\item{qc_metric}{Character string specifying the QC metric column name to analyze (default: "sum_gene")}

\item{samples}{Character string specifying the sample ID column name (default: "sample_id")}

\item{mad_threshold}{Numeric value for MAD threshold for outlier detection (default: 3)}

\item{edge_threshold}{Numeric threshold for edge detection (default: 0.6, reduced from 0.75)}

\item{min_cluster_size}{Minimum cluster size for morphological cleaning (default: 20, reduced from 40)}

\item{shifted}{Logical indicating whether to apply coordinate adjustment for hexagonal arrays (default: FALSE)}

\item{batch_var}{Character specifying batch variable for outlier detection ("slide", "sample_id", or "both", default: "both")}

\item{name}{Character string for naming output columns (default: "edge_dryspot")}
}
\value{
A SpatialExperiment object with additional columns in colData:
\item{\link{name}_edge}{Logical indicating spots identified as edges}
\item{\link{name}_problem_id}{Character identifying problem area clusters}
\item{\link{name}_problem_size}{Numeric size of problem area clusters}
}
\description{
This function identifies edge dryspots and problem areas in spatial
transcriptomics data by analyzing QC metrics and spatial patterns.
Enhanced with parameterized thresholds and improved sensitivity.
}
\examples{
# Enhanced usage with improved sensitivity
\dontrun{
spe <- detectEdgeDryspots(spe, edge_threshold = 0.5, min_cluster_size = 15)

# More aggressive detection
spe <- detectEdgeDryspots(spe, edge_threshold = 0.4, min_cluster_size = 10)

}
}
